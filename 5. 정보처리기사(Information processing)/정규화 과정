비정규화되어 있는 데이터
↓

도메인이 원자값
1정규화(1NF) 완료
↓

1정규화 만족하고 부분적 함수 종속 제거
2정규화(2NF) 완료
↓

2정규화 만족하고 이행적 함수 종속 제거
3정규화(3NF) 완료
↓

3정규화를 만족하고 결정자이면서 후보키 아닌것 제거
강한 3정규화(BCNF) 완료
↓

강한 3정규화 만족하고 다치 종속
4정규화(4NF) 완료
↓

4정규화 만족하고 조인 종속성 이용
5정규화(5NF) 완료
